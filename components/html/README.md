# UIKit HTML Markup Component

## Introduction
Adds a SDC HTML Markdown component which supports UIKit markup and classes.

## Features & Usage
Create block using the provided 'HTML' paragraph type, and in the 'HTML Body' field add
component markup to use a [UIKit component](https://getuikit.com/docs/introduction).
