name: Nav
group: DGI Components
description: Nav component
props:
  type: object
  properties:
    items:
      title: Nav Items
      description: The items for the navigation component.
      type: array
    auto_gen:
      title: Auto Generate
      description: Whether to auto-generate the navigation items.
      type: boolean
libraryOverrides:
  dependencies:
    - exhibit_components/uikit
